# Cursor 本机 Agent（推荐）
CURSOR_API_KEY=
CURSOR_MODEL=grok-4.6
CURSOR_MODEL_EFFORT=high
CURSOR_MODEL_FAST=true

# SQLite 数据目录（默认项目根下的 data/，重启和重新 build 都不会删）
# OPS_DATA_DIR=

# 使用外部 OpenAI 兼容接口时可选
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini
